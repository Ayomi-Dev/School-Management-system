import {ParentDashboardLayout}  from '@/src/components/layouts';

const ParentLayout = ({ children }: { children: React.ReactNode }) => {
  return <ParentDashboardLayout>{children}</ParentDashboardLayout>;
}
export default ParentLayout;
